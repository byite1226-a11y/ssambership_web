node_modules
.next
.env
.env.local
.env.*.local
.vercel
.DS_Store
*.log

.env.local.production-backup
*.production-backup
*.tsbuildinfo
screenshots/

# 보고서 생성 스크립트 (개발용, 배포 불필요)
generate_report.mjs
*.docx

# 스크린샷 스크립트
scripts/

# E2E / Playwright 산출물 (소스 아님)
/test-results/
/playwright-report/

# 임시 스크린샷·캡처 산출물 (소스 아님 — 커밋 금지)
/tmp-screens/

# 스크린샷 캡처 결과물·아카이브 (소스 아님 — 커밋 금지, 백업은 별도)
screenshots_final/
screenshots_*/
screenshots*.zip
shots_*.zip
